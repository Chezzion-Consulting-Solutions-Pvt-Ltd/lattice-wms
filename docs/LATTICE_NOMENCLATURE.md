# Lattice Nomenclature

Use Lattice-native model names and `lattice_*` database tables.

The current location model is `Bay` with table `lattice_bay`. Do not introduce alternate location table names or `kv_*` names.

External system terms are integration mappings only. SAP EWM storage bin terminology, Manhattan terms, and Dematic terms must not drive internal model names.
